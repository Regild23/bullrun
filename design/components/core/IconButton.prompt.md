Compact icon-only button. Always pass `label` for accessibility.

```jsx
<IconButton label="Notifiche" variant="surface"><BellIcon/></IconButton>
```

Variants: `solid` (gradient), `surface`, `ghost`. Sizes `sm | md | lg`.
