Dark rounded surface — the default container for everything. Add `glow` for emphasis (focus/active), `accent` to tint the border with an asset-class color, `interactive` for hover lift.

```jsx
<Card glow="teal">Patrimonio totale …</Card>
<Card accent="var(--br-crypto)" interactive>Crypto module</Card>
```
