Metric readout with mono tabular numerals and a colored up/down delta. Use `size="xl"` + `glowValue` for the hero portfolio number.

```jsx
<Stat label="Patrimonio totale" value="€ 12.480" delta="+4.2%" size="xl" glowValue />
```
Direction is inferred from the delta sign; override with `direction`.
