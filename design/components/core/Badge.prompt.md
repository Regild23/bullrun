Small uppercase mono pill for statuses and tags. Use `up`/`down` for market deltas, asset tones for class labels.

```jsx
<Badge tone="up" dot>+4.2%</Badge>
<Badge tone="purple">Crypto</Badge>
<Badge tone="warn" soft={false}>Evento</Badge>
```
