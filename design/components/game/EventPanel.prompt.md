The "Evento del mese" card — explains the news that moves prices each turn, with per-asset impact chips. Amber alert accent.

```jsx
<EventPanel title="La BCE alza i tassi" icon={<NewsIcon/>}
  body="Le banche centrali aumentano i tassi di interesse…"
  impacts={[{label:'BTP',delta:'+3%'},{label:'Azioni',delta:'-5%',dir:'down'}]} />
```
